[deploy]
preDeployCommand = "python railway_migrate_safe.py"
startCommand = "python -m uvicorn dgn_picks_api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
