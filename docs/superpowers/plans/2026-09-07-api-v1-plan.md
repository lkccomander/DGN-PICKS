# API v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expose the approved M2 DGN-PICKS API v1 contract over the Railway-hosted FastAPI service.

**Architecture:** Keep route handlers thin. Use a shared SQLAlchemy session dependency, focused query/application helpers, and Pydantic v2 schemas. Preserve existing `/api/health` while mounting the new contract under `/api/v1`.

**Tech Stack:** Python 3.11+, FastAPI, Pydantic v2, SQLAlchemy 2, Alembic, pytest, httpx, PostgreSQL.

## Global Constraints

- No authentication in this milestone.
- Use deterministic local fixtures before any live odds provider.
- Normalize provider payloads at the provider boundary.
- Odds history is append-oriented; never overwrite historical snapshots.
- Every pick belongs to a user.
- Seed users are `gato`, `daran`, and `noch`; do not create `noche`.
- All 13 initial demo picks belong to `gato`.
- Preserve unresolved sports data; do not invent the Malakai Toney pick side, dates, opponents, or odds.
- Store timestamps in UTC and keep secrets out of source control.

### Task 1: API schemas and database dependency

**Files:**
- Create: `apps/api/src/dgn_picks_api/api/v1/schemas.py`
- Create: `apps/api/src/dgn_picks_api/api/v1/dependencies.py`
- Test: `apps/api/tests/test_api_v1.py`

**Interfaces:**
- `get_db() -> Generator[Session, None, None]`
- `PickCreate(user: str, game_id: int, market_id: int, selection_id: int | None, stake_units: Decimal, notes: str | None)`
- Response schemas for users, games, markets, snapshots, picks, analytics, and `SeedReport`.

- [ ] Write schema tests for valid pick payloads, missing required fields, and invalid numeric values.
- [ ] Run `py -m pytest apps/api/tests/test_api_v1.py -v`; expect failures because the module and schemas do not exist.
- [ ] Implement Pydantic schemas with `ConfigDict(from_attributes=True)` where ORM serialization is used.
- [ ] Implement `get_db` using `SessionLocal`, yielding once and closing in `finally`.
- [ ] Run the focused tests and confirm they pass.

### Task 2: Read-only users, games, markets, and odds routes

**Files:**
- Create: `apps/api/src/dgn_picks_api/api/v1/routes/users.py`
- Create: `apps/api/src/dgn_picks_api/api/v1/routes/games.py`
- Create: `apps/api/src/dgn_picks_api/api/v1/routes/markets.py`
- Modify: `apps/api/src/dgn_picks_api/main.py`
- Test: `apps/api/tests/test_api_v1.py`

**Interfaces:**
- `GET /api/v1/users?user=...`
- `GET /api/v1/games?date=YYYY-MM-DD&conference=...`
- `GET /api/v1/games/{game_id}`
- `GET /api/v1/games/{game_id}/markets`
- `GET /api/v1/markets/{market_id}/history`

- [ ] Add tests for user listing, game listing, `date` and `conference` filters, game 404s, market listing, and snapshot history ordering.
- [ ] Implement routes with SQLAlchemy `select()` statements and deterministic ordering by kickoff or observation time.
- [ ] Resolve team conference filtering through the game's home or away team relationship without guessing missing data.
- [ ] Return `404` for unknown game and market IDs.
- [ ] Mount the routers under `/api/v1` without changing `/api/health`.
- [ ] Run the read-route tests and confirm they pass.

### Task 3: Pick creation and pick listing

**Files:**
- Create: `apps/api/src/dgn_picks_api/api/v1/routes/picks.py`
- Create: `apps/api/src/dgn_picks_api/domains/picks/service.py`
- Modify: `apps/api/src/dgn_picks_api/api/v1/schemas.py`
- Test: `apps/api/tests/test_api_v1.py`

**Interfaces:**
- `create_pick(session: Session, payload: PickCreate) -> Pick`
- `GET /api/v1/picks?user=...&date=YYYY-MM-DD`
- `POST /api/v1/picks`

- [ ] Add tests for a valid pick, user filtering, date filtering, unknown user/game/market/selection references, and malformed bodies.
- [ ] Implement `create_pick` to validate all references and ensure the selection belongs to the market and game.
- [ ] Copy the current snapshot's line and odds into the new pick at creation time.
- [ ] Reject unresolved selections with HTTP `422` and never create a playable pick for them.
- [ ] Commit the pick and related transaction once; convert domain validation failures to HTTP `422`.
- [ ] Add a test that inserts a later odds snapshot and verifies the pick's taken price is unchanged.
- [ ] Run the pick tests and confirm they pass.

### Task 4: Analytics and development seed route

**Files:**
- Create: `apps/api/src/dgn_picks_api/api/v1/routes/analytics.py`
- Create: `apps/api/src/dgn_picks_api/api/v1/routes/dev.py`
- Modify: `apps/api/src/dgn_picks_api/main.py`
- Modify: `apps/api/src/dgn_picks_api/seed/service.py`
- Test: `apps/api/tests/test_api_v1.py`

**Interfaces:**
- `GET /api/v1/analytics/summary`
- `POST /api/v1/dev/seed -> SeedReport`

- [ ] Add tests for aggregate wins, losses, pushes, total units risked, profit, ROI, and repeated seed calls.
- [ ] Implement analytics from persisted picks using the existing Decimal calculation functions.
- [ ] Return `null` ROI when total units risked is zero.
- [ ] Run the existing transactional seed service through the dev route and return its report without exposing credentials.
- [ ] Confirm the seed route preserves the exact users and unresolved Malakai definition rules.
- [ ] Run the analytics and seed tests and confirm they pass.

### Task 5: Migration and integration validation

**Files:**
- Modify: `apps/api/migrations/env.py`
- Modify: `apps/api/migrations/versions/0004_align_m2_models.py`
- Modify: `README.md`
- Test: `apps/api/tests/test_migrations.py`

- [ ] Wire `Base.metadata` into Alembic and import every model module before configuring `target_metadata`.
- [ ] Add migration smoke tests that verify the head revision creates all model tables and that the alignment migration is reversible.
- [ ] Validate with `py -m alembic -c apps/api/alembic.ini upgrade head --sql`.
- [ ] Run `py -m pytest apps/api/tests` and confirm all tests pass.
- [ ] Document `/api/v1` examples, seed behavior, and the Railway `DATABASE_URL` requirement.
- [ ] Run web lint/build where dependencies are available and record any environment-only limitation.

## Self-review checklist

- Every endpoint in the approved design has a task.
- Error behavior is explicit: `404` for unknown resources and `422` for invalid bodies or references.
- Pick taken-price preservation and append-only odds history are tested explicitly.
- No live provider, authentication, or unrelated frontend work is included.
- Railway deployment is treated as the runtime target while local fixtures remain deterministic.
